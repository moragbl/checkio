#!/usr/bin/python

sum = 0
array = []

for a in range (0, len(array)):
    print a
    print array[a]
    if not a%2:
        sum = sum+array[a]
        print "sum is now ", sum
if array[]:
    sum = sum * array[-1]

print "sum is ", sum
